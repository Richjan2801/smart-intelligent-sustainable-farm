{
  "name": "sisf",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "start:dev": "npm run build && npm run live",
    "server": "npm run dev --prefix backend",
    "build": "platformio run --target upload --environment esp32doit-devkit-v1",
    "monitor": "platformio device monitor --environment esp32doit-devkit-v1",
    "live": "concurrently -n \"firmware,backend ,frontend\" -c \"yellow,blue,green\" \"platformio device monitor --environment esp32doit-devkit-v1\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}